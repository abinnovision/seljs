# sel-checker

Static type-checker for SEL expressions. Takes a schema, hydrates a type-checking CEL environment, and validates expressions returning validity and inferred types.

Depends on sel-types and sel-schema.
